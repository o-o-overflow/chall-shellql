id: "shellql" # this needs to be a single word, no spaces
title: "shellql" # this is the title that will show up on the scoreboard
description: |
  Do you even SQL? The flag is in the table `flag`

  http://b9d6d408.quals2018.oooverflow.io
# list of files that should be public (these will be made available via the scoreboard)
public_files: [
  "./public_files/shellme.so",
]
tags: ["web", "shellcode"] # list of tags
author: "adamd"
stateful: false # true|false two consecutive connections go to the same VM
to_deploy: true # true|fale this chall needs a VM to be deployed
is_ready: false # true|false this chall is ready for prime time
